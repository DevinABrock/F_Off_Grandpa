import React from 'react'
import keyWordList from './keyWordList'

function scoring(userText, grandpaText) {
    let playerScore = 0;
    console.log(userText)
}
scoring("hello", "goodbye")
export default scoring

